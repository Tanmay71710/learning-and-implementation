apiVersion: apps/v1
kind: Deployment
metadata:
  name: script-execution-manager
spec:
  replicas: 5
  template:
    spec:
      containers:
      - name: script-execution-manager
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"