apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: artifactory-production
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  labels:
    environment: production
    app: artifactory
spec:
  project: script-execution-manager
  source:
    repoURL: https://github.com/Tanmay71710/learning-and-implementation
    targetRevision: master
    path: environments/production/artifactory-deployment.yaml
  destination:
    server: https://production.kubernetes.example.com
    namespace: artifactory-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true