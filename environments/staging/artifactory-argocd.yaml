apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: artifactory-staging
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  labels:
    environment: staging
    app: artifactory
spec:
  project: script-execution-manager
  source:
    repoURL: https://github.com/Tanmay71710/learning-and-implementation
    targetRevision: staging
    path: environments/staging/artifactory-deployment.yaml
  destination:
    server: https://staging.kubernetes.example.com
    namespace: artifactory-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true