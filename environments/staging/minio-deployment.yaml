apiVersion: v1
kind: ConfigMap
metadata:
  name: minio-config
  namespace: minio-staging
  labels:
    app: minio
    environment: staging
data:
  MINIO_ROOT_USER: minioadmin
  MINIO_ROOT_PASSWORD: staging-minio-admin
  MINIO_ENDPOINT: http://minio:9000
  MINIO_BUCKET: script-execution-manager-staging
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: minio
  namespace: minio-staging
  labels:
    app: minio
    environment: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: minio
  template:
    metadata:
      labels:
        app: minio
        environment: staging
    spec:
      containers:
      - name: minio
        image: minio/minio:latest
        ports:
        - containerPort: 9000
        env:
        - name: MINIO_ROOT_USER
          valueFrom:
            configMapKeyRef:
              name: minio-config
              key: MINIO_ROOT_USER
        - name: MINIO_ROOT_PASSWORD
          valueFrom:
            configMapKeyRef:
              name: minio-config
              key: MINIO_ROOT_PASSWORD
        command:
        - /bin/sh
        - -c
        - minio server /data --console-address ":9001"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: minio-pvc-staging
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-pvc-staging
  namespace: minio-staging
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 20Gi
---
apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: minio-staging
  labels:
    app: minio
    environment: staging
spec:
  type: LoadBalancer
  selector:
    app: minio
  ports:
  - name: api
    port: 9000
    targetPort: 9000
  - name: console
    port: 9001
    targetPort: 9001